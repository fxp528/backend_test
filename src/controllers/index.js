function importData(res, req) {
  throw 'not implement'
}
function fetchCount(res, req) {
  throw 'not implement'
}
function fetchList(res, req) {
  throw 'not implement'
}

export { importData, fetchCount, fetchList };